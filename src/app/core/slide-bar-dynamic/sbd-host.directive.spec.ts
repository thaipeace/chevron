import { SbdHostDirective } from './sbd-host.directive';

describe('SbdHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SbdHostDirective();
    expect(directive).toBeTruthy();
  });
});
