import { ArrangeKeyValuePipe } from './arrange-key-value.pipe';

describe('ArrangeKeyValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrangeKeyValuePipe();
    expect(pipe).toBeTruthy();
  });
});
