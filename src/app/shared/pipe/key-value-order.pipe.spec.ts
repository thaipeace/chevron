import { KeyValueOrderPipe } from './key-value-order.pipe';

describe('KeyValueOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyValueOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
