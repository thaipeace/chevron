import { SpinLoadingDirective } from './spin-loading.directive';

describe('SpinLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
