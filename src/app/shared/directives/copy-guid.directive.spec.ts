import { CopyGUIDDirective } from './copy-guid.directive';

describe('CopyGUIDDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyGUIDDirective();
    expect(directive).toBeTruthy();
  });
});
