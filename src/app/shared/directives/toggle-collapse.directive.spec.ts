import { ToggleCollapseDirective } from './toggle-collapse.directive';

describe('ToggleCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
