import * as _ from 'lodash';
import {DefaultObject} from '@shared/models/default/default-object.model';

export class StationProductInventoryHistoryModel extends DefaultObject {
  constructor(_data = {}) {
    super(_data, 'sysId');
  }
}

